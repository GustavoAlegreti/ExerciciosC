
//Explicação sobre o que é variavel.

#include <stdio.h>

int main() {

  int a = 1;
  
// Uma variável é um nome associado a um local de armazenamento na memória do computador. Declarada especificando o tipo de dados, atribui-se valores usando o operador de atribuição. A relação entre variáveis e memória é que o compilador reserva espaço de armazenamento na memória para os valores das variáveis. Durante a execução, o programa acessa e manipula esses valores utilizando os endereços de memória associados às variáveis.
}